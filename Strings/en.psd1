@{
    Sections       = 'SECTIONS'
    SelectSection  = 'Select a section'
    StatsFmt       = '{0} settings · {1} applied'
    RebootFmt      = 'Restart required after: {0}'
    BtnApply       = 'Apply section'
    BtnRevert      = 'Revert'
    BtnRefresh     = 'Refresh status'
    BtnNextStep     = 'Next step'
    BtnRevertStep   = 'Revert step'
    BtnCardButtonsOnly = 'Use card buttons'
    Open           = 'Open'
    OpenSettings   = 'Open Settings'
    Apply          = 'Apply'
    Revert         = 'Revert'
    ManualOnly     = 'manual only'
    Done           = 'done ✓'
    NotAdmin       = 'not admin'
    PwshMissingBadge = 'PowerShell 7 missing'
    PwshMissingTooltip = "The app is running through Windows PowerShell 5.1. This is a supported fallback; install portable PowerShell 7 with the bottom button for a self-contained toolkit."
    PwshPortableMissingBadge = 'portable PowerShell 7 missing'
    PwshPortableMissingTooltip = "Portable PowerShell 7 is not installed in Engine\PowerShell\. The app can use system PowerShell; the portable runtime makes the toolkit self-contained."
    ErrApplyFmt    = "Error applying '{0}':`n{1}"
    ErrFailedFmt   = 'Failed: {0}'
    LangSwitchTo   = 'RU'
    SelFmt         = '  ·  {0} selected'
    BtnCleanApp    = 'Clear data'
    BtnDocs        = 'Documentation'
    BtnDocsShort   = 'DOCS'
    BtnReadme      = 'README'
    BtnInstallPwsh = 'Install PowerShell'
    BtnDownloadPage = 'Download page'
    TooltipDownloadPage = 'Open the official download page in your browser'
    TooltipDocsButton = 'Open the complete documentation folder'
    TooltipReadmeButton = 'Open README'
    TooltipPdfButton = 'Open PDF'
    TooltipInstallPwshButton = 'Install portable PowerShell 7'
    TooltipLogButton = 'Show or hide the log'
    TooltipTerminalButton = 'Show or hide the terminal'
    TooltipTerminalMaxButton = 'Expand or restore the terminal'
    TerminalWaitingOutput = 'Command is still running; output may arrive in bursts...'
    TerminalStillWorkingFmt = 'Still working — {0} without output. This is normal while a package downloads; Cancel stops the step.'
    BtnApplyBusy    = 'Applying…'
    BtnRevertBusy   = 'Reverting…'
    BtnCancel       = 'Cancel'
    OpCancelling    = 'Cancelling — finishing the current step…'
    OpCancelled     = 'Cancelled'
    OpBusy          = 'An operation is already running — please wait.'
    OpBusyNotStartedFmt = 'An operation is already running — "{0}" was not started.'
    BtnCleanConfirm = "Clear all backups and the session log?`nApp state resets to fresh — cannot be undone."
    ProgressFmt     = '{0} / {1}'
    OrderedFlowSuffix = '  ·  ordered'
    OrderedNoPending  = 'No pending steps in this ordered workflow.'
    OrderedNoApplied  = 'No applied steps to revert in this ordered workflow.'
    CardsOnlySuffix = '  ·  card buttons only'
    CardsOnlyLog    = 'This subsection uses individual card buttons only.'
    # ── Install verdicts (Terminal panel) ──────────────────────────────────────
    # Every install run must end on one plain line — including "nothing to do" and "it failed" —
    # so a card can never look like it is stuck.
    InstallWingetMissing         = 'winget (App Installer) was not found — install "App Installer" from the Microsoft Store, then use this button again. Nothing was changed.'
    InstallFoundOutsideWingetFmt = 'Already present on this system: {0} {1}. Windows manages this component itself, so winget does not list it.'
    InstallSkippedAlreadyPresent = 'Nothing to install — the component is already in place. This step is done; move on to the next one.'
    InstallCurrentVersionFmt     = 'Installed version: {0}. Checking for an update…'
    InstallNotPresentYet         = 'Not installed yet — starting the installation.'
    InstallUpgradeFellBack       = 'winget could not upgrade the existing entry — trying a fresh install.'
    InstallDone                  = 'Installed successfully.'
    InstallUpdated               = 'Updated to the latest version.'
    InstallAlreadyLatest         = 'The latest version is already installed — no update is needed. This step is done.'
    InstallAlreadyInstalled      = 'This version is already installed — the installer stopped on its own. This step is done.'
    InstallNoPackageFound        = 'winget found no matching package for this operation.'
    InstallMultipleFound         = 'winget found several matching packages and did not pick one. Nothing was changed.'
    InstallHashMismatch          = 'The downloaded installer did not match its published checksum, so winget refused to run it. Nothing was changed — try again later or install from the vendor page.'
    InstallDownloadFailed        = 'The download failed — check the network connection. Nothing was changed.'
    InstallRequiresAdmin         = 'This package needs administrator rights. Restart the app as administrator and try again.'
    InstallNoApplicableInstaller = 'This package has no installer for this edition of Windows or this architecture. Nothing was changed.'
    InstallSystemNotSupported    = 'This package does not support this system. Nothing was changed.'
    InstallNoSources             = 'No winget sources are configured. Run "winget source reset --force" in a terminal, then try again.'
    InstallCancelledByUser       = 'Cancelled — the installer was stopped. Nothing is left running in the background.'
    InstallTimedOutFmt           = 'No response for {0} minutes — winget was stopped so the app does not stay stuck. Nothing is running in the background; you can retry or continue with the other steps.'
    InstallCouldNotStartFmt      = 'Could not start winget: {0}. Nothing was changed — you can continue with the other steps.'
    InstallFailedCodeFmt         = 'winget exited with code {0}. The step did not complete — see the winget message above. Nothing is running in the background; you can continue with the other steps.'
    UninstallDone                = 'Removed successfully.'
    UninstallNotInstalled        = 'winget does not list this package as installed — there is nothing to remove.'
    InstallerScriptMissingFmt    = 'Installer not found: {0}. Nothing was changed.'
    InstallerScriptDone          = 'The installer finished successfully.'
    InstallerScriptFailedFmt     = 'The installer exited with code {0}. The step did not complete — see the output above. Nothing is running in the background.'
    InstallerScriptTimedOutFmt   = 'The installer did not respond for {0} minutes and was stopped so the app does not stay stuck. Nothing is running in the background.'
    UsbPowerNoDevices            = 'This machine exposes no power-manageable devices — there is nothing to change.'
    UsbPowerBeforeFmt            = 'Power-manageable devices: {0}; power saving already off on {1}.'
    UsbPowerAfterFmt             = 'Power saving is now off on {0} of {1} devices.'
    UsbPowerNoChange             = 'Nothing changed — the devices were already in this state, or the change was refused.'
    UsbPowerNothingToRestore     = 'No record of devices switched off by this card — nothing to restore.'
    UsbPowerRestoredFmt          = 'Power saving restored on {0} of {1} devices this card had switched off.'
    ElevationDone                = 'File permissions granted.'
    ElevationFailedFmt           = 'Could not grant permissions (code {0}). The step did not complete.'
    StepNeedsAdmin               = 'This step changes system-wide settings and needs administrator rights. Restart the app as administrator. Nothing was changed.'
    RevertServiceFailedFmt       = 'Could not restore service {0}: {1}. The backup was kept, so you can try again.'
    RevertServiceStateFailedFmt  = 'Service {0} was set back to its previous startup type, but its running state could not be restored: {1}. A reboot will settle it.'
    FeatureStateUnreadable       = 'The feature state could not be read back after the change - see the output above for what Windows reported.'
    RevertNoBackup               = 'This was already set before the program first saw it, so there is no recorded previous value to put back. Nothing was changed - switch it off in Windows Settings if you want it off.'

    TooltipWhat   = 'What it does'
    TooltipHow    = 'How to do it'
    TooltipNote   = 'Note'
    TooltipManualFallback = 'Complete this step manually, then mark the card as done.'
}
